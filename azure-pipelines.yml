# trigger:
# - master

stages:
- stage: 'build'
  displayName: build   
  
  jobs:
  - template: pipeline-templates/build.yml
    parameters:
      projectName: "LSG.GenericCrud"
      configuration: "Release"
  - template: pipeline-templates/build.yml
    parameters:
      projectName: "LSG.GenericCrud.Dto"
      configuration: "Release"
- stage: 'deployment'
  dependsOn: 'build'
  jobs:
  - template: pipeline-templates/release.yml
    parameters:
      projects:
      - projectName: "LSG.GenericCrud"
      - projectName: "LSG.GenericCrud.Dto"